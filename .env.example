# Контейнер ComfyUI. За замовчуванням використовується офіційний образ з GHCR.
# Перш ніж запускати docker compose, виконайте `./scripts/login-ghcr.sh`,
# щоб авторизуватися в GHCR з токеном (scope read:packages). Без логіну
# отримаєте помилку `denied` під час завантаження образу.
COMFYUI_IMAGE=ghcr.io/comfyanonymous/comfyui:latest

# Якщо доступ до GHCR заблокований, розкоментуйте альтернативний публічний образ.
# COMFYUI_IMAGE=cocktailpeanut/comfyui-docker:latest

# RIFE для профілю "rife". Можна замінити на власний образ.
RIFE_IMAGE=ghcr.io/k4yt3x/video2x:latest
