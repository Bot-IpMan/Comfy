# Ім'я локального образу, який збирається автоматично.
COMFYUI_IMAGE=comfyui:local

CLI_ARGS="--listen --port 8188 --cpu"

# Вихідний репозиторій та гілка/тег/коміт для клонування під час збірки Dockerfile
COMFYUI_REPO=https://github.com/comfyanonymous/ComfyUI.git
COMFYUI_REF=master

# Альтернативне джерело коліс PyTorch (за замовчуванням стабільна збірка cu121 для CUDA 12.1).
TORCH_INDEX_URL=https://download.pytorch.org/whl/cu121

# Якщо віддаєте перевагу попередньо зібраному образу, замініть COMFYUI_IMAGE та видаліть секцію build у docker-compose.yml.
# COMFYUI_IMAGE=ghcr.io/comfyanonymous/comfyui:latest

# RIFE для профілю "rife". Можна замінити на власний образ.
RIFE_IMAGE=ghcr.io/k4yt3x/video2x:latest

# Якщо драйвер NVIDIA на хості старіший за 525.60, рекомендуємо оновити його через apt (наприклад, пакет nvidia-driver-535).
