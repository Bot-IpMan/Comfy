# Локальні додаткові залежності (за бажанням).
# Основні пакети ComfyUI вже встановлюються з /opt/ComfyUI/src/requirements.txt (див. Dockerfile).
# Сюди можеш додати, наприклад:
#   opencv-python-headless==4.10.0.84
#   onnxruntime==1.17.3
#   safetensors>=0.4.2
# Якщо файл порожній — збірка просто пропустить цей крок.
